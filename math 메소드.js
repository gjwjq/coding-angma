let num = 5.1

// console.log(
//   Math.ceil(num), //올림
//   Math.floor(num), //내림
//   Math.round(num) //반올림
// );

let longNum = 123.123123

// console.log(longNum.toFixed(8)) //소숫점 반환
let asdf = 'sdf'
// console.log(isNaN(asdf)) // 숫자면 false 문자면 true

let margin = '10px'
// console.log(parseInt(margin)); // 읽을수있는데까지 숫자로 반환 (처음 시작이 숫자여야함)

let redColor = 'c3'

// console.log(parseInt(redColor, 16)) // 16진수로 변환해줌
// console.log(parseInt('11', 2)); // 진법계산몰라서이해못함

// console.log(parseInt(Math.random()*100+1)); 이러면 1~100 사이의 랜덤값 뽑아줌

// Math.max,min 최댓값, 최솟값 구해줌

const maxMath = [1,2,3,4,5]
// console.log(Math.max(...maxMath)); //배열이라서 스프레드로 

// console.log(Math.pow(2, 10)); // 첫번째수를 두번째수만큼 거듭제곱

// console.log(Math.sqrt(16)); // 걍 루트 씌워줌
